{
  "name": "hlv-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "sirv dist --host 0.0.0.0 --port ${PORT:-3000} --single"
  },
  "dependencies": {
    "astro": "^5.7.10",
    "sirv-cli": "^3.0.1"
  }
}
